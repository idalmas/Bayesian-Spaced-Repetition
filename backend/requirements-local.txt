# Local Development Dependencies (Heavy - ~800MB+)
# Use this if you want to run embeddings locally without an OpenAI API key
#
# Usage:
#   pip install -r requirements-local.txt
#   export EMBEDDING_PROVIDER=local
#   python app.py

# Flask web framework for REST API
flask==3.0.0
flask-cors==4.0.0

# HTTP client (still included for flexibility)
requests>=2.28.0

# NumPy for vector math (cosine similarity)
numpy==1.26.4

# Sentence embeddings for card similarity (HEAVY - ~800MB with torch)
sentence-transformers==3.3.0

