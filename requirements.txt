# Root Requirements - Vercel Python Dependencies (Lightweight)
# Uses OpenAI embeddings instead of sentence-transformers to stay under 250MB limit
#
# Set environment variables in Vercel:
#   - OPENAI_API_KEY: Your OpenAI API key
#   - EMBEDDING_PROVIDER: "openai" (default) or "local"

# Flask web framework for REST API
flask==3.0.0
flask-cors==4.0.0

# HTTP client for OpenAI API calls
requests>=2.28.0

# NumPy for vector math (cosine similarity)
numpy==1.26.4
